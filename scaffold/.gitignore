# Claude Code agent artifacts — keep these out of version control.
# Merge (append) these lines into an existing repo .gitignore; don't overwrite.
.claude/worktrees/
.claude/settings.local.json

# Recurring agent scratch / generated files (2026-06-22)
UPGRADE_INTENT.md
lint_output.*
*local-test-screenshot*
