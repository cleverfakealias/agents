# llms.txt
# Machine-readable index for AI agents entering this repository.
# Format: key: value pairs, one per line. Blank lines and lines starting with # are ignored.
# Spec reference: https://llmstxt.org

project: <!-- project name -->
description: <!-- project purpose/description -->
language: <!-- language (TypeScript, Python, Rust, Go, etc.) -->
framework: <!-- framework (React, FastAPI, Astro, etc.) -->
runtime: <!-- runtime (Node.js, Python, Cloudflare Workers, etc.) -->

# --- Agent Instructions ---

system-prompt-dir: .agents/
global-rules: .agents/global_core.md
project-context: .agents/project_context.md
shim-claude: .agents/shims/claude.md
shim-openai: .agents/shims/openai.md
shim-gemini: .agents/shims/gemini.md
shim-copilot: .agents/shims/copilot.md
shim-cursor: .agents/shims/cursor.md
shim-windsurf: .agents/shims/windsurf.md
assembly-order: shim + global_core + project_context

# --- Key Source Locations ---

# Entry points / main application files
entry-pages: <!-- directory for main routes/pages, e.g. src/pages/ -->
components: <!-- directory for UI components, e.g. src/components/ -->
utils: <!-- directory for utilities, e.g. src/lib/ -->
styles: <!-- stylesheet location, e.g. src/styles/global.css -->

# API / backend specifics
api-routes: <!-- API route directory, if applicable, e.g. src/pages/api/ -->
api-endpoint: <!-- main API endpoint or handler, if applicable -->

# Configuration files
config: <!-- comma-separated list, e.g. tsconfig.json, package.json, vite.config.ts -->

# Data / knowledge
data: <!-- data directory, e.g. src/data/ -->

# --- Commands ---

dev: <!-- command to start local dev server, e.g. npm run dev -->
build: <!-- command to build for production, e.g. npm run build -->
test: <!-- command to run tests, e.g. npm run test -->
typecheck: <!-- command for type checking, e.g. npm run typecheck -->
lint: <!-- command to lint, e.g. npm run lint -->
deploy: <!-- command or process for deployment, e.g. npm run deploy -->

# --- Optional Layers ---
# Uncomment the keys for whichever layers the skill scaffolded. Drop the others.

# Architecture — Mermaid topology + ADRs.
# architecture-dir: .agents/architecture/
# architecture-system: .agents/architecture/system.mmd
# architecture-dataflow: .agents/architecture/dataflow.mmd
# architecture-deployment: .agents/architecture/deployment.mmd
# adr-dir: .agents/architecture/decisions/

# Intents — spec-driven work units with binding scope.
# intents-dir: .agents/intents/
# intents-open: .agents/intents/open/
# intents-in-flight: .agents/intents/in-flight/
# intents-done: .agents/intents/done/

# Nested AGENTS.md — per-directory context overrides.
# nested-agents-md: enabled

# --- MCP Tools (optional) ---
# List MCP servers configured for this project. Agents prefer these over raw shell commands.
# mcp-server-filesystem: src/ (read, write)
# mcp-server-postgres: read, schema-inspect
# mcp-server-github: read PRs/issues; no push

# --- Do Not Modify ---

do-not-touch: <!-- lockfiles and generated dirs, e.g. pnpm-lock.yaml, dist/, .next/, build/ -->

# --- Secrets Policy ---

secrets-policy: Local via secret loader (dotenvx, direnv, doppler, op://). Prod via platform secret store (GitHub Secrets, Cloudflare, AWS Secrets Manager). Code references env var NAMES only — never values.
agent-no-read: .env, .env.*, .envrc, .dev.vars*, secrets.*, *.pem, *.key, *.p12, *.pfx, id_rsa*, .npmrc, .pypirc, ~/.aws/credentials, ~/.config/gcloud/, gha-creds-*.json, .terraformrc
