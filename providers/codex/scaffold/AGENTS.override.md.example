# <Service or package name>

<!--
  EXAMPLE OVERRIDE FILE — rename to AGENTS.override.md inside a subdirectory.
  Codex concatenates AGENTS.md and AGENTS.override.md files from repo root down to CWD,
  with closer-to-CWD content winning on conflict.

  Use this file for service- or package-specific rules that override the root AGENTS.md.

  Delete this `.example` file from the root; only create real `AGENTS.override.md` files
  inside the subdirs that need them.
-->

## Stack (overrides root)

- <!-- e.g., FastAPI 0.115 + Pydantic v2 + asyncpg -->

## Commands (overrides root)

```bash
<!-- service-specific test/run, e.g., uv run pytest packages/api -->
<!-- service-specific dev, e.g., uv run uvicorn packages.api.main:app --reload -->
```

## Conventions specific to this service

- <!-- service-only rules; do not restate root rules -->

## Do NOT touch in this subtree

- <!-- e.g., generated migrations dir, pinned dataset files -->
