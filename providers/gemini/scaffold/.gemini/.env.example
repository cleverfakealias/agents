# Template for .gemini/.env (which should be gitignored).
# Copy to .env, fill in actual values locally.
# Gemini's mcpServers config reads env vars from this file when launched with --env-file or via shell export.
#
# Only variable NAMES live in version control. Never commit the real .env.

GITHUB_TOKEN=
