{
  "_comment": "Project-scoped MCP servers. Commit this file. The init-claude-standards skill will prune servers the user opts out of. Tool names surface as mcp__<server>__<tool> — use that exact form in .claude/settings.json permissions.",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
